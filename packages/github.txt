# ==============================================================================
# github.txt — Tools installed directly from GitHub releases or install scripts
# ==============================================================================
# These are tools that aren't available in mise, Flatpak, Homebrew, or system
# repos — they must be fetched directly from their source.
#
# Format: one tool name per line (matched by module 05-github.sh).
# Comment a line out to skip that tool.
#
# To add a new tool: add its name here AND add a matching _install_<name>()
# function in modules/05-github.sh.
# ==============================================================================

# Anthropic Claude Code CLI — AI coding assistant (installed via official script)
claude-code

# DevPod — Dev Container manager that works with any IaC backend (Docker, Podman, SSH, cloud)
# Installed as a static binary from GitHub releases (no package manager needed)
devpod

# OpenAI Codex CLI — AI coding agent from OpenAI, installed via npm
# Requires node@lts in mise.txt (already added)
codex

# Nerd Fonts Symbols Only — icon glyphs used by terminal tools (starship, waybar, yazi, etc.)
# Skipped on Arch — ttf-nerd-fonts-symbols is installed via pacman instead
nerd-fonts
